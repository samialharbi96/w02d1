const sum =function ( a,b){

  c = a + b ;
  return z;
}


const avr = function(a,b){

  c=(a+b)/2
  return c;
};


const findFactorial = function(num){
  if(num <= 0 )
  console.log("Error! Factorial for negative number  or zero");
  }


  else {
    let fact = 1;
    for (i = 1; i <= num; i++) {
        fact *= i;}
    return fact ;}


  onst round = function (num){
    b = Math.round(num)
     return b};


 const toThePowerOf = function (a, b) {
 let c= Math.pow(a,b)
 return z ;
 }
 
 const randomNumber = function getRandom() {
 
 return Math.random();
 }
 function getRandomArbitrary() {
     return Math.round(Math.random());
 
   }
   const randomRange = function (max) {
 
     return Math.floor(Math.random() * max); 
   }
   const includeOf = function(str,cha) {
 for (let i = 0; i < str.length; i++) {
 if(str[i].includeOf (cha)){
 return true
 }
     return fales;
 }